// TempPage.xaml.cs — code-behind for TempPage
// This is a placeholder page used during development — no real logic here
// Unlike other pages, the ViewModel is set directly in TempPage.xaml (not injected here)

namespace StarterApp.Views;

public partial class TempPage : ContentPage
{
	public TempPage()
	{
		InitializeComponent(); // builds the UI from TempPage.xaml
	}
}